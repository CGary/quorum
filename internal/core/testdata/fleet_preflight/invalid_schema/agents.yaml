transports:
  codex:
    binary: codex
    argv_template:
      - --json
    input_channel: stdin
    output_format: jsonl
    timeouts:
      default_s: 120
    quota_class: subscription
    tier: 2
    models:
      openai/gpt-5.5-medium:
        model_arg: gpt-5.5
        reasoning_effort: medium
    contract_test: contracts/codex.yaml
    active: true
