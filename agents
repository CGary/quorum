#!/usr/bin/env bash
# Quorum Agent CLI Wrapper (uv version)

# Ensure we are in the project root
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export PYTHONPATH="$DIR/.agents:$PYTHONPATH"

exec uv run python3 -m cli.main "$@"
