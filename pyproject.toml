[project]
name = "quorum"
version = "0.1.0"
description = "Autonomous agent framework for task specification and execution."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pyyaml>=6.0.3",
]
